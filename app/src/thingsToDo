- Save button
    - When clicked -> record the audiotracks in mp3 format
    - Go into AudioTrack
        - this.fileData contains the buffer (this.fileData.audioBuffer)
            - Create offlineaudiocontext and record it
            - Encode it using the mp3 encoder ( Can get to this point )
                - How to store it into the database